import React from 'react'
import ReactDOM from 'react-dom'

function App(): React.ReactElement {
  return (
    <p>React Extension App!</p>
  )
}

ReactDOM.render(<App />, document.getElementById('root'))
